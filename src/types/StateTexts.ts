export type StateTexts = {
    add?: string,
    link?: string,
    open?: string,
    info?: string,
    password?: string,
    mandatory?: string,
    error?: string,
    warn?: string,
    log?: string,
    reset?: string,
}